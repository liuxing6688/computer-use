# Computer Use

在 Claude Code CLI 中用自然语言操控本机 Windows 桌面的工具集。模型通过一个常驻的本地服务观察屏幕并注入输入。

## Language

### 感知

**感知通道**:
从桌面获取可定位目标的一种途径。不同通道产出同一种结构，可互相替换。
_Avoid_: 模式、后端、driver

**像素通道**:
以窗口截图感知界面、并以截图像素坐标指称位置的感知通道。
_Avoid_: 视觉模式、截图模式、vision mode

**元素通道**:
以 Windows UI Automation 可及性树感知界面的感知通道。目标应用没有可用的可及性树时不可用。
_Avoid_: UIA 模式、结构化模式、a11y 模式

**目标**:
界面上一个可被单独指称的对象，具有描述与边界框。由哪个感知通道产出不影响它的用法。
_Avoid_: 元素、控件、节点、widget

**观察**:
一次对当前桌面状态的采集结果，由目标清单与截图组成，并带有采集时刻的元数据。
_Avoid_: 快照、snapshot、屏幕状态

### 操作

**动作**:
模型发出的一次桌面操作，例如点击某个坐标、输入文本、按下按键。
_Avoid_: 指令、命令、操作步骤

**只读工具**:
只产生观察、不改变桌面状态的工具。
_Avoid_: 查询工具、感知工具

**输入工具**:
向桌面注入鼠标或键盘输入的工具。
_Avoid_: 写工具、执行工具、操作工具

**危险动作**:
执行前必须经人确认的动作，三类：逃逸出任务作用域的、后果难以撤销的、后果离开本机的。
_Avoid_: 高风险操作、敏感操作

**外发动作**:
后果离开本机、他人可见的动作，例如发送消息、提交表单、发起支付。危险动作中最不可挽回的一类。
_Avoid_: 对外操作、不可逆操作

**拦截**:
在动作抵达桌面之前将其暂停、交由人裁决的过程。
_Avoid_: 审批、确认流程、gate

### 边界

**任务作用域**:
本次任务开始时声明的一组窗口。动作落在其中无需拦截，落到其外即为危险动作。
_Avoid_: 目标应用、焦点窗口、白名单

**高危窗口**:
无论是否在任务作用域内，动作落入即须拦截的窗口，例如终端、系统设置、Agent 自身所在的窗口。
_Avoid_: 黑名单窗口、敏感窗口

**急停**:
人立即中止 Agent 一切输入、并使输入工具转入拒绝状态的手段。
_Avoid_: 熔断、kill switch、紧急停止
